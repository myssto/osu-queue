// ADMIN
// (Default: Parent) Sets whether created channels go in a seperate category or remain in the parent category

// ARG
// (req) category_mode: str [Parent, New, Specified]
// (opt) category: category