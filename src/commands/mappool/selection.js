// ADMIN
// Choose how a pool is decided for the match

// ARG
// (req) type: str [Vote, Random, Random From Global, Enforced]
// global_avgsr: str (Float? Ignored if type != Random From Global)
// enforced_pool: str (Ignored if type != Enforced)